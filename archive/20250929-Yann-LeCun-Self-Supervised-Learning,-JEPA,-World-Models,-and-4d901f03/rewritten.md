# Yann LeCun：自监督学习、JEPA、世界模型与AI的未来

## 内容评分

**综合评分：88/100　强烈推荐**

| 维度 | 得分 | 评分依据 |
|------|------|----------|
| AI 相关性 | —/40 |  |
| 故事性 | 18/30 | 演讲结构清晰，从问题到解决方案再到开放挑战，逻辑递进。使用了生动的类比（科学史、婴儿学习）和具体demo，但整体偏技术演讲，叙事张力中等。 |
| 加分项 | —/30 |  |

## 核心观点

- 当前AI（LLM）的推理方式——固定层数的前馈传播——在计算上受限，无法高效表示复杂函数。
- 自回归生成（逐个预测token）本质上是发散过程，导致幻觉无法根本避免。
- 人类和动物的学习主要靠自监督学习，而非监督或强化学习，且依赖世界模型进行规划和推理。
- JEPA（联合嵌入预测架构）通过预测表示而非像素，避免了生成模型在高维连续空间中的不可解问题。
- 层次化的世界模型和规划是实现通用智能的关键，但如何训练这样的系统仍是开放问题。

## 关键洞察

## 为什么这期内容值得关注

Yann LeCun 是深度学习三巨头之一，他的观点往往代表着对主流范式的深刻反思。在LLM如日中天的2025年，他公开呼吁“放弃生成模型”、“不要研究LLM”，这种逆流而上的姿态本身就极具价值。更重要的是，他不仅批评，还给出了具体的替代方案——JEPA和基于世界模型的认知架构。这期内容不是空谈，而是对AI未来十年研究方向的路线图式思考。

## 最值得深想的一个问题

LeCun 提出的核心矛盾是：为什么机器能通过律师资格考试，却无法像猫一样灵活地抓取物体？他的答案是：我们误判了“智能”的难度。那些对人类来说“难”的事情（下棋、解微积分、写文章）其实算法上相对简单，因为它们是离散符号操作；而那些对人类和动物来说“简单”的事情（理解物理世界、规划动作序列）却需要处理连续、高维、不确定的信号，这正是当前AI的软肋。这个反直觉的洞察直接解释了为什么自动驾驶和家用机器人迟迟无法落地——我们一直在用错的方法解决错的问题。

## 对我们的启示

对于AI从业者和投资者，LeCun 的建议非常具体：不要把所有资源押注在LLM的scale-up上。文本数据的红利正在耗尽（一个四岁孩子看到的数据量已经等于LLM的训练数据），而视频和物理世界的数据才是下一波金矿。但关键在于，不能直接用生成式方法预测像素（那是不可能的任务），而要用JEPA这类架构学习抽象表示。对于创业者，这意味着：谁能率先构建出可用的世界模型，谁就能在机器人、自动驾驶、具身智能领域占据先机。对于普通读者，这期内容提供了一个判断AI进展的框架：不要被“通过律师考试”这种新闻迷惑，真正衡量智能的标准是——它能不能像10岁孩子一样，第一次就学会收拾餐桌。

## 深度摘要

## 背景：这场对话从哪里出发

Yann LeCun 是卷积神经网络（CNN）的发明者、2018年图灵奖得主，现任Meta首席AI科学家。在哈佛CMSA的这场演讲中，他直面当前AI（尤其是大语言模型）的根本局限，并提出了一条通往更智能机器的路径：自监督学习、联合嵌入预测架构（JEPA）和世界模型。他的核心判断是：我们离人类级别的智能还很远，甚至离猫级别的智能都很远。

## 核心论点

### 当前AI的三大缺陷

LeCun 首先指出，当前主流AI架构（LLM）存在三个根本性问题。第一，**推理能力受限**：它们通过固定层数的前馈传播计算输出，而很多函数无法用这种方式高效表示。第二，**自回归生成必然发散**：逐个预测token的过程就像在树上行走，一旦走错分支就无法回头，导致幻觉。数学上，正确序列的概率随长度指数衰减。第三，**训练数据错位**：一个四岁孩子通过视觉系统接收的数据量（约10^14字节）已经等于最大的LLM训练所用的文本量，但孩子学到的是物理世界的常识，而LLM只学到了文本表面的统计规律。

### 为什么生成模型在物理世界行不通

LeCun 用大量篇幅论证了一个关键观点：**在连续、高维的物理信号（如图像、视频）上做生成式预测是不可行的**。如果你试图预测视频的下一帧像素，由于未来本质上是不确定的，最优解只能是所有可能未来的模糊平均。即使引入潜变量，也无法处理自然视频中不可预测的细节（比如观众的面孔、地板的纹理）。因此，他提出：**不要在像素空间预测，要在表示空间预测**。

### JEPA：联合嵌入预测架构

JEPA 的核心思想是：训练一个编码器将输入（如部分视频）映射到抽象表示，再训练一个预测器在表示空间中预测未来状态的表示。这样，系统可以自动忽略那些不可预测的细节，只保留对预测有用的信息。LeCun 用科学史上的例子类比：要预测木星100年后的位置，你不需要知道木星表面的所有细节，只需要6个数字（位置和速度）。同样，好的表示应该“扔掉”无关信息，只保留可预测的结构。

### 世界模型与规划

有了JEPA，就可以构建世界模型——给定当前状态和假设动作，预测下一状态的表示。然后，通过优化（搜索）找到能最小化任务成本的动作序列，这就是规划。LeCun 强调，这种“推理即优化”的范式比LLM的“前馈传播”更强大，因为它允许系统花更多时间解决更难的问题（类似人类的系统二思维）。他还展示了初步成果：用DINO（一种自监督方法）训练的编码器，结合简单的预测器，已经能让机器人通过规划完成零样本任务，如移动棋子或导航到目标。

### 层次化世界模型：未解决的挑战

LeCun 最后指出，真正的智能需要层次化的世界模型——在不同时间尺度和抽象级别上做规划。比如，从纽约去巴黎，你不能在毫秒级的肌肉控制层面规划，而需要在“去机场→坐飞机”这样的高层抽象上规划。如何让系统自动学习这些层次，以及如何在层次间做规划，目前是完全开放的问题。

## 关键案例 / 数据

- **数据量对比**：一个4岁孩子通过视觉系统接收约10^14字节数据，等于最大的LLM训练数据量。但孩子学到的是物理世界常识，而LLM只学到文本统计。
- **预测模糊问题**：10年前的实验显示，用生成式模型预测视频下一帧，结果总是模糊的，因为系统只能预测所有可能未来的平均。
- **DINO模型**：2025年初，自监督方法DINO在图像理解上首次超越监督学习，且所需标注数据更少。
- **机器人零样本规划**：LeCun 团队用DINO编码器+预测器，让机器人通过规划（而非强化学习）完成移动棋子和导航任务，无需针对任务训练。

## 结论与行动框架

LeCun 的最终建议直截了当：**放弃生成模型，转向JEPA；放弃强化学习（至少作为主要方法），转向基于世界模型的规划；不要研究LLM，研究JEPA**。对于AI研究者，这意味着应该把精力放在如何训练层次化世界模型、如何设计不崩溃的自监督学习算法上。对于投资者和创业者，这意味着：关注那些在视频理解、机器人、自动驾驶领域使用JEPA或类似架构的团队，他们可能正在解决真正的智能问题。对于普通读者，记住一个判断标准：如果一个AI系统只能处理文本，它离真正的智能还很远；如果它能像婴儿一样通过观察物理世界学习常识，那才是真正的突破。

---
*基于 YouTube | Harvard CMSA 转录自动生成，处理时间：2026-06-22*
